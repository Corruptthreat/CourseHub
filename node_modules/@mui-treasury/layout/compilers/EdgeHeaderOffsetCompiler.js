"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _Header = _interopRequireDefault(require("../effects/Header"));

var _utils = require("../utils");

var _sidebarChecker = require("../utils/sidebarChecker");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var _default = function _default(edgeSidebar, header) {
  return {
    getResultStyle: function getResultStyle(sidebarId) {
      var result = {};
      var found = false;
      var configMap = edgeSidebar.configMapById[sidebarId];

      if (configMap) {
        var breakpoints = (0, _utils.combineBreakpoints)(configMap, header);
        breakpoints.forEach(function (bp) {
          var sidebarConfig = (0, _utils.pickNearestBreakpoint)(configMap, bp);
          var headerConfig = (0, _utils.pickNearestBreakpoint)(header, bp);

          if (sidebarConfig) {
            if (headerConfig && (0, _Header["default"])(headerConfig).isObjectClipped(sidebarId) && (0, _sidebarChecker.isCollapsibleSidebarConfig)(sidebarConfig)) {
              found = true;
              result[bp] = {
                height: headerConfig.initialHeight
              };
            } else if (found) {
              found = false;
              result[bp] = {
                height: 0
              };
            }
          }
        });
      }

      return result;
    }
  };
};

exports["default"] = _default;