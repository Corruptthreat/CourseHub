function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

import React from 'react';
import { useTheme } from '@material-ui/core';
import { useLayoutCtx } from '../../contexts';
import { createBreakpointStyles } from '../../utils';
import ContentCompiler from '../../compilers/ContentCompiler';
import { generateStyledProxyCreator } from '../Shared/StyledProxy';
export default (function (styled) {
  var styledProxy = generateStyledProxyCreator(styled);
  var StyledFooter = styledProxy('footer');

  var Footer = function Footer(props) {
    var _useTheme = useTheme(),
        breakpoints = _useTheme.breakpoints;

    var _useLayoutCtx = useLayoutCtx(),
        data = _useLayoutCtx.data,
        state = _useLayoutCtx.state;

    var styles = createBreakpointStyles( // can reuse content compiler at this point
    ContentCompiler(state, data.edgeSidebar).getResultStyle(data.footer.id), breakpoints);
    return /*#__PURE__*/React.createElement(StyledFooter, _extends({}, props, {
      styles: _extends({
        transition: 'all 225ms'
      }, styles)
    }));
  };

  return Footer;
});