export default (function () {
  var id;
  return {
    create: function create(footerId) {
      id = footerId;
    },
    getData: function getData() {
      return {
        id: id
      };
    },
    debug: function debug() {
      if (process.env.NODE_ENV !== 'production') {
        console.group('Footer:', "\"".concat(id, "\""));
        console.groupEnd();
      }
    }
  };
});