import { useWindowCtx } from '../contexts';
import { DEFAULT_HEADER_ID } from '../utils';
export var useHeaderHeight = function useHeaderHeight() {
  var headerId = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : DEFAULT_HEADER_ID;

  var _useWindowCtx = useWindowCtx(),
      iDocument = _useWindowCtx.iDocument;

  var headerElm = iDocument ? iDocument.querySelector("[mui-layout='".concat(headerId, "']")) : null;

  if (headerElm) {
    return headerElm.clientHeight;
  }

  return 0;
};
export default useHeaderHeight;