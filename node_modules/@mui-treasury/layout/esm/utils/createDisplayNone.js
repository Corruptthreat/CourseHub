export default (function (hidden, breakpoints) {
  if (!hidden || !breakpoints) return {};
  var mediaQueries = {};
  hidden.forEach(function (bp) {
    mediaQueries[breakpoints.only(bp)] = {
      display: "none"
    };
  });
  return mediaQueries;
});