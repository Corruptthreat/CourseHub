import { subtractCalc, get } from '../../../utils';
export default (function (config, headerConfig) {
  var isHidden = config && config.hidden;
  return {
    getRootStyle: function getRootStyle() {
      if (isHidden) return {
        display: 'none'
      };
      return {
        width: config.width
      };
    },
    getBodyStyle: function getBodyStyle() {
      if (isHidden) return {
        display: 'none'
      };
      return {
        position: 'absolute',
        top: 0,
        width: '100%',
        padding: 'unset',
        margin: 'unset',
        overflow: 'auto',
        height: subtractCalc('100vh', get(headerConfig, 'initialHeight', 0))
      };
    }
  };
});